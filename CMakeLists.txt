project (Math)

set(GENERATE_MATH_CODE FALSE)
if (GENERATE_MATH_CODE)
	file(READ vec2_template.h TEMPLATE_TEXT)
	string(REPLACE "TYPENAME" "vec2" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "float" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0.0f" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/vec2.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "ivec2" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "int" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/ivec2.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "uvec2" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "unsigned" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/uvec2.h CONTENT "${TEXT}")

	file(READ vec3_template.h TEMPLATE_TEXT)
	string(REPLACE "TYPENAME" "vec3" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "float" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0.0f" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/vec3.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "ivec3" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "int" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/ivec3.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "uvec3" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "unsigned" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/uvec3.h CONTENT "${TEXT}")

	file(READ vec4_template.h TEMPLATE_TEXT)
	string(REPLACE "TYPENAME" "vec4" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "float" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0.0f" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/vec4.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "ivec4" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "int" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/ivec4.h CONTENT "${TEXT}")

	string(REPLACE "TYPENAME" "uvec4" TEXT "${TEMPLATE_TEXT}")
	string(REPLACE "TYPE" "unsigned" TEXT "${TEXT}")
	string(REPLACE "ZERO" "0" TEXT "${TEXT}")
	file(GENERATE OUTPUT ${CMAKE_CURRENT_LIST_DIR}/uvec4.h CONTENT "${TEXT}")
endif()